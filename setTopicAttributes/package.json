{
    "name": "set-topic-attributes",
    "version": "1.0.0",
    "description": "Contains Lambda code to set topic attributes",
    "main": "index.js",
    "scripts": {
        "clean": "rm -rf dist/ cdk.out/ coverage/",
        "build": "tsc",
        "build:watch": "tsc -w",
        "pre-package": "run-s build && cp package*.json dist/ && cd dist && npm ci --production && rm package*.json && cd ..",
        "test": "jest --coverage",
        "test:watch": "jest --coverage --watchAll"
    },
    "author": "Milan Makwana",
    "license": "ISC",
    "dependencies": {
        "@aws-cdk/aws-sns": "^1.54.0",
        "@aws-cdk/core": "^1.54.0",
        "aws-sdk": "^2.719.0",
        "ts-custom-error": "^3.1.1"
    },
    "devDependencies": {
        "@aws-cdk/assert": "^1.54.0",
        "@types/jest": "^26.0.7",
        "aws-cdk": "^1.54.0",
        "jest": "^26.1.0",
        "npm-run-all": "^4.1.5",
        "ts-jest": "^26.1.3",
        "typescript": "^3.9.7"
    }
}
